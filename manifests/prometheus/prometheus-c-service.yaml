apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus-c-reader
  name: prometheus-c-reader
  namespace: monitoring
spec:
  ports:
  - name: web
    port: 9090
    protocol: TCP
    targetPort: 9090
  selector:
    # app: prometheus-c-reader
    prometheus: prometheus-c-reader
  type: ClusterIP