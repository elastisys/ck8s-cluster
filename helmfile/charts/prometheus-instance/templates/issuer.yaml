{{- if .Values.ingress.enabled -}}
{{- if .Values.ingress.issuer.selfSigned -}}
apiVersion: certmanager.k8s.io/v1alpha1
kind: Issuer
metadata:
  name: self-signed
spec:
  selfSigned: {}
{{- end }}
{{- end }}
