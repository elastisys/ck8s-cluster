ingress:
  clusterDomain: {{ requiredEnv "ECK_BASE_DOMAIN" }}

grafana:
  url: "https://grafana.{{ requiredEnv "ECK_BASE_DOMAIN" }}"
  password: {{ requiredEnv "CUSTOMER_GRAFANA_PWD" }}
  apiKeyName: "ck8sdash-wc"
  datasource: "k8s-metrics"
elastic: 
  apiKeyName: "ck8sdash-key-wc"
