ingress:
  clusterDomain: {{ requiredEnv "ECK_OPS_DOMAIN" }}

grafana:
  url: "https://grafana.{{ requiredEnv "ECK_OPS_DOMAIN" }}"
  password: "{{ requiredEnv "GRAFANA_PWD" }}"
  apiKeyName: "ck8sdash-wc"
  datasource: "prometheus-ss"
