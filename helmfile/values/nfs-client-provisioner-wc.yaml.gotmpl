resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

storageClass:
  defaultClass: true
serviceAccount:
  name: nfs-client-provisioner
nfs:
  path: "/nfs"
  server: {{ requiredEnv "NFS_WC_SERVER_IP" }}